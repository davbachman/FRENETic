Original prompt: Build a new web app game that teaches users about curvature and torsion of 3D curves. The app shows a minimap closed curve, a first-person tunnel following that curve, pointer-as-trackpad steering, radar history with curvature/torsion arrows, blue curvature and green torsion meters, authored levels from planar waves to torus knots, momentum in steering, and a dark spaceship HUD motif.

Implementation notes:
- Use Vite + TypeScript + Three.js.
- Maintain deterministic hooks: window.advanceTime(ms) and window.render_game_to_text().

Task 9 notes:
- Added a focused TunnelRings TDD test first; initial run failed because src/game/rendering/tunnel.ts did not exist.
- Implemented colors, TunnelRings, GameRenderer, and main.ts app wiring. Targeted tunnel test now passes.
- Browser check used the develop-web-game Playwright client against Vite on 127.0.0.1:4174. Start-mode screenshot shows cyan tunnel rings and render_game_to_text() returns mode/level/player state.
- Note: when entering play mode with no steering for several frames, the existing simulation quickly leaves the centerline and the first-person camera can point away from the sampled tunnel; steering/HUD work is expected in later tasks.
- Task 9 review fix: added a failing test for stable ring position attribute/array identity across repeated TunnelRings.update() calls, then changed TunnelRings to preallocate position buffers and mutate them in place without per-segment Vector3 allocations.

Task 10 notes:
- Added focused HUD helper tests first. Initial targeted run fails because src/game/rendering/hud.ts is not implemented yet, establishing the RED step for Task 10.
- Implemented HudOverlay with canvas texture, top blue/green meters, bottom-left minimap, pointer-aligned radar, compact status text, and renderer same-canvas overlay pass.
- Verification: targeted HUD tests, full Vitest suite, typecheck, and production build pass. Playwright screenshots written under /private/tmp/frenetic-task10-hud and /private/tmp/frenetic-task10-hud-playing.
- Browser note: start-mode screenshot shows tunnel rings plus HUD. Playing-mode HUD remains visible and reports steering history; the pre-existing Task 9 camera/simulation drift can still leave the tunnel out of view after several frames.
- Task 10 review fix: preserved signed torsion in the top-right meter, kept mobile HUD panels/text separated at 320x568 without changing pointer-aligned radar coordinates, and anchored the red radar vector at the current steering endpoint. Added helper tests for each review finding.

Task 11 notes:
- Added a Starfield points shell to the scene that follows the player/camera, plus fullscreen-aware F/Escape keyboard handling.
- Added focused tests for Starfield geometry/material/disposal and fullscreen/Escape keyboard behavior.
- Verification: targeted Starfield/app tests, full Vitest suite, typecheck, and production build pass. Playwright screenshots written under /private/tmp/frenetic-task11-start and /private/tmp/frenetic-task11-playing; start shows tunnel rings, HUD, and stars, while playing shows the expected carry-forward no-steering tunnel drift with HUD/starfield still visible.

Verification:
- npm run test:run passed.
- npm run typecheck passed.
- npm run build passed.
- Web-game steering check ran at http://127.0.0.1:5173/ with screenshots in output/web-game/steer-right and no console error artifact. It rendered the minimap, radar, blue curvature meter, and green torsion meter, and render_game_to_text() reported 91 steering trace points with smoothed steering changed from neutral. The known playing-mode drift caused the final state to be mode "failed" with the main tunnel out of view.
- Web-game level cycling check ran at http://127.0.0.1:5173/ with screenshots in output/web-game/level-cycle and no console error artifact. The app still rendered HUD panels and a valid authored level state, but the develop-web-game client did not transmit the requested "n" inputs because its button map currently supports only up/down/left/right/enter/space/a/b, so this run did not prove authored level cycling.

Task 12 fix notes:
- Added a tested B key alias for next level so the develop-web-game client's supported button map can exercise level cycling while preserving the existing N shortcut.
- Updated level-cycle.json to use the B alias, and updated steer-right.json to a shorter axis-aligned steering burst verified against the planar first level.
- Verification after fixes: npm run test:run passed, npm run typecheck passed, and npm run build passed.
- Web-game steering check ran at http://127.0.0.1:5174/ with screenshots in output/web-game/steer-right and no console error artifact. Screenshot shows visible tunnel rings, readable minimap, radar trace, blue curvature meter, green torsion meter, and desktop text fitting inside panels. render_game_to_text() reported mode "playing", 91 steering trace points, and smoothed steering changed from neutral.
- Web-game level cycling check ran at http://127.0.0.1:5174/ with screenshots in output/web-game/level-cycle and no console error artifact. render_game_to_text() reported mode "playing" on authored level index 3, "cinquefoil-knot", with HUD panels visible. The main tunnel was out of view in the final level-cycle screenshot due to the known playing-mode drift, but the minimap, radar, blue curvature meter, green torsion meter, and panel text remained visible/readable.
- Final Task 12 verification update: shortened level-cycle.json to a single B press so the client proves a non-initial authored level without drifting the tunnel out of view. Re-ran npm run test:run, npm run typecheck, and npm run build successfully. Web-game checks ran at http://127.0.0.1:5173/ with no console error artifacts. Steering ended mode "playing" with 87 steering trace points and non-neutral smoothed steering; level-cycle ended mode "playing" on authored level index 1, "lifted-wave", with tunnel rings, minimap, radar, blue curvature meter, green torsion meter, and panel text visible/readable.
- Final level-cycle tightening: updated level-cycle.json to three one-frame B presses to exercise cycling through authored levels. JSON parse check passed. Web-game level-cycle check ran at http://127.0.0.1:5173/ with --pause-ms 0, no console error artifact, and screenshots in output/web-game/level-cycle. render_game_to_text() reported mode "playing" on authored level index 3, "cinquefoil-knot"; screenshot showed tunnel rings visible at the left edge plus minimap, radar, blue curvature meter, green torsion meter, and readable panel text.

Final review fix notes:
- Added failing tests for deterministic browser time and the renderer recovery camera target, then fixed both review blockers.
- window.advanceTime/app.advanceTime now takes ownership of simulation time by stopping the live RAF loop before deterministic stepping, so Playwright frame counts are no longer inflated by wall-clock RAF frames.
- GameRenderer now uses a centerline recovery look target when the player drifts far from the tunnel, keeping the authored tunnel rings visible instead of showing only stars/HUD while the game remains in playing mode.
- Verification after final review fixes: npm run test:run passed with 78 tests, npm run typecheck passed, and npm run build passed.
- Web-game steering and level-cycle checks ran at http://127.0.0.1:5173/ with no console error artifacts. Steering ended mode "playing" with 31 steering trace points, health 1, non-neutral smoothed steering, and visible tunnel/HUD. Level-cycle ended mode "playing" on authored level index 3, "cinquefoil-knot", with deterministic trace count 1 and visible knot tunnel/HUD.

Cinematic HUD UI pass notes:
- Implemented the approved focused cinematic HUD pass: expanded neon/glass palette, larger hybrid top meters, volumetric three-layer tunnel rings, angular minimap/radar pods, and an integrated lower console rail.
- Added focused helper tests for cinematic meter layout, narrow viewport panel separation at 280x568, meter track geometry, lower rail geometry, and layered tunnel ring allocation/reuse.
- Review fixes completed during implementation: narrow HUD layout now hard-caps panel widths by available space, and TunnelRings.update() uses indexed loops for layered ring updates to avoid per-frame callback allocation.
- Verification: npm run typecheck passed, npm run test:run passed with 85 tests, and npm run build passed.
- Web-game steering check ran at http://127.0.0.1:5173/ with screenshots in output/web-game/cinematic-hud-steer and no console error artifact. State reported mode "playing", level "Planar Wave", HUD visible fields, and 31 steering trace points.
- Web-game level-cycle check ran at http://127.0.0.1:5173/ with screenshots in output/web-game/cinematic-hud-level-cycle and no console error artifact. State reported mode "playing" on authored level index 3, "cinquefoil-knot", with HUD visible fields.
- Compact viewport check ran at 360x640 with screenshots in output/web-game/cinematic-hud-mobile and no console errors. Meters, minimap, radar pod, and lower rail remained separated and readable.
- Final review fix: tunnel rings now stay cyan on all levels instead of inheriting level visual tint, and TunnelRings.update() no longer allocates a Three.js Color each frame. Re-ran tunnel tests, typecheck, full test suite, and build successfully. Re-ran the level-cycle browser check and confirmed the Cinquefoil Knot tunnel rings render cyan with no console error artifact.
- Final responsive status fix: added medium-width status placement tests for 560, 768, and 1024 pixels plus a wide desktop inline-status test. calculateStatusTextY now moves status below the meter row whenever the center gap is too narrow. Re-ran HUD tests, typecheck, full test suite with 87 tests, build, and a 768x720 browser capture in output/web-game/cinematic-hud-medium with no console errors.

Tunnel reference correction notes:
- Reworked the previous flat annulus tunnel bands into rounded torus-like neon tube rims with a tight bright core and two low-opacity glow shells, so the tunnel reads as luminous outlines against black gaps instead of cyan walls.
- Moved the camera look target farther down the sampled centerline, which restores a receding circular tunnel composition closer to the uploaded ring reference instead of a side-on stripe pattern.
- Kept HUD inset panel fills solid black through the shared hudColors.panelFill value.
- Verification: npm run typecheck passed, npm run test:run passed with 88 tests, and npm run build passed. Browser captures were refreshed in output/web-game/start and output/web-game/current.
- Follow-up visual reset: added a real dark opaque tunnel wall mesh behind the neon rims, shifted tunnel ring color to a less saturated blue-cyan, and reduced rim/halo thickness and opacity. Browser capture in output/web-game/current now shows a dark tunnel surface with thin luminous rings instead of open starfield gaps between bright cyan bands.
- Verification after wall reset: npm run typecheck passed, npm run test:run passed with 89 tests, and npm run build passed.
- Second follow-up visual reset: replaced the rounded 3D tube rims with flat, very narrow annular rim strips, lowered ring opacity again, and shifted ring color to #5cbce8. The refreshed output/web-game/current capture now shows dim thin blue rings on the wall instead of bright cyan ring bands. Verification: npm run typecheck passed, npm run test:run passed with 95 tests, and npm run build passed.
- User clarified the tunnel center should stay open, not be filled; straight-ish tunnel segments should show more rings receding into the distance. Removed the attempted aperture-fill direction, increased ring count to 78, thickened the core/halo rim strips, raised near and far core opacity, and kept only the dark wall mesh as the non-ring tunnel surface. Fresh browser capture is in output/web-game/thicker-rings-20260523-0005. Verification: npm run typecheck passed, npm run test:run passed with 96 tests, and npm run build passed.
- Startup failure fix: added a 3-second default collision grace period so pressing Enter does not immediately fail the run before the player can begin steering. Warning/progress still update during the grace window; direct simulation configs without the optional grace value retain immediate collision damage behavior. Added an app regression proving 2.5 seconds after start remains playing with high health. Browser was reloaded/restarted and showed H:100 at the same early progress where it previously failed. Verification: npm run typecheck passed, npm run test:run passed with 97 tests, and npm run build passed.
- Restart drift fix: reproduced that neutral start/restart still flew straight out of the first curved tunnel after about 3.5 seconds, leaving the camera looking side-on at the tube even though the restart initially reset correctly. Added failing simulation/app regressions, then added a short default centerline launch assist so the player rides the tunnel centerline during the startup/restart orientation window before normal steering takes over. Verification: targeted player/app tests passed, npm run typecheck passed, npm run test:run passed with 99 tests, and npm run build passed. In-app browser verification at http://127.0.0.1:5173/ showed failure at 61% with the restart prompt, then Enter restart followed by 3.5 seconds stayed H:100 with a forward tunnel view instead of the previous side-on drift. Note: the standalone develop-web-game Playwright client stalled on this app during the long restart-assist frame burst and was stopped; browser verification used the in-app browser directly.
- Half-speed tuning: added a failing authored-level speed regression, then reduced all authored level speeds by 50%: Planar Wave 7 -> 3.5, Lifted Wave 7.4 -> 3.7, Trefoil Knot 7.8 -> 3.9, Cinquefoil Knot 8.2 -> 4.1. Verification: targeted sampler test passed, npm run typecheck passed, npm run test:run passed with 100 tests, and npm run build passed. Reloaded the in-app browser at http://127.0.0.1:5173/ so the dev server serves the updated constants.
- Half-speed follow-up fix: user reported the speed did not feel changed. Verified the dev server was serving the reduced speeds, then measured one second of deterministic gameplay and found progress was still 0.089 instead of the intended 0.063. Root cause was the centerline launch assist basing each assisted step on quantized nearest-sample progress, which advanced roughly one sample per frame and masked the halved speed. Added a failing regression for assisted launch progress, fixed assist to use elapsed time times authored level speed, and updated the restart failure test for the slower run. Verification: targeted speed/app tests passed, npm run typecheck passed, npm run test:run passed with 101 tests, npm run build passed, and the web-game runtime speed check now reports progress 0.064 after 61 frames.
- Input/momentum usability fix: user reported game still too hard and radar/meters appeared dead. Reproduced that HUD instruments update when dragging inside the radar, so the issue was input affordance: normal pointer/trackpad movement over the main canvas was ignored unless the drag began inside the radar panel. Added passive canvas pointer steering around a centered virtual trackpad while keeping radar click-drag control, and reduced default steeringResponseSeconds from 0.18 to 0.08 to remove excess lag. Verification: targeted pointer/app tests passed, npm run typecheck passed, npm run test:run passed with 103 tests, npm run build passed, and in-app browser was forced to http://127.0.0.1:5173/?fresh=20260523-0758; moving the pointer over the main canvas visibly drove the radar vector and curvature meter.
- Further speed reduction: user still experienced the game as too fast. Runtime measurement showed the current build moved about 0.064 of level 1 per second, a roughly 16-second lap, which is still too fast for learning. Reduced authored speeds another 50% to quarter-original values: 1.75, 1.85, 1.95, 2.05. Updated progress assertions for the slower pace. Verification: targeted sampler/player/app tests passed, npm run typecheck passed, npm run test:run passed with 103 tests, npm run build passed, and the web-game runtime speed check at http://127.0.0.1:5173/?fresh=20260523-0804 now reports 0.032 progress after 61 frames. In-app browser was forced to the same fresh URL.
- Demo mode pass: added a default non-interactive `demo` mode because the tuned play mode is still not usable enough for learning. Demo mode advances the camera/player exactly along the sampled centerline by arc length, wraps progress around the closed loop, keeps health/distance/warning locked to safe values, and ignores steering history. Pressing Enter from demo resets to the level start and switches to actual playing mode. The bottom-right HUD pod is now labeled `NORMAL PLANE`; while in demo it traces the tangent-derivative vector in the curve normal plane and uses the sampled centerline curvature/torsion for the top meters.
- Demo mode verification: added failing tests first for default demo state, demo centerline stepping/wrapping, Enter-to-play reset, text-state normal-plane visibility, and normal-plane tangent-derivative projection. Targeted suites passed after implementation. Fresh verification passed: npm run typecheck, npm run test:run with 112 tests, and npm run build. Web-game client checks ran at http://127.0.0.1:5173/?fresh=20260523-demo and http://127.0.0.1:5173/?fresh=20260523-demo-enter with screenshots in output/web-game/demo-mode and output/web-game/demo-enter; demo state reported health 1, distanceFromCenterline 0, steeringTracePoints 0, curvature 0.409, torsion 0, and normalPlaneVisible true. The in-app browser was refreshed to http://127.0.0.1:5173/?fresh=20260523-demo-live and render_game_to_text() reported mode demo after deterministic advancement.
- Demo level follow-up: changed default demo preview to use the fourth authored level, `Cinquefoil Knot`, while preserving Enter-to-play as a reset to level 1 `Planar Wave`. Added failing tests first for the level-4 demo default and level-1 play handoff. Verification passed: targeted state/app tests, npm run typecheck, npm run test:run with 113 tests, and npm run build. Web-game client checks ran at http://127.0.0.1:5173/?fresh=20260523-demo-level4 and http://127.0.0.1:5173/?fresh=20260523-demo-level4-enter; demo state reported mode demo, level index 3, id `cinquefoil-knot`, health 1, centerline distance 0, and nonzero torsion, while the Enter check reported mode playing on `planar-wave`. The in-app browser was refreshed to http://127.0.0.1:5173/?fresh=20260523-demo-level4-live.
- Normal-plane vector follow-up: replaced the bottom-right trace/steering readout with a current-sample Frenet normal-plane readout. It draws only two centered vectors: blue `T'` with length proportional to curvature and green projected `N'` with length/sign proportional to torsion. Removed the old clockwise/counterclockwise torsion arc path and fading trace from the HUD renderer. Added failing HUD helper tests first for centered `T'` and projected `N'` vectors plus clamping. Verification passed: targeted HUD tests, npm run typecheck, npm run test:run with 113 tests, and npm run build. Web-game capture ran at http://127.0.0.1:5173/?fresh=20260523-normal-plane-vectors-final with screenshot/state in output/web-game/normal-plane-vectors-final; state stayed demo on `cinquefoil-knot` with health 1 and centerline distance 0. The in-app browser was refreshed to http://127.0.0.1:5173/?fresh=20260523-normal-plane-vectors-final-live.
- Demo-only screensaver pass: removed app-level player interaction wiring so the shipped app is now a passive curvature/torsion visualization. The app no longer constructs PointerTrackpad, registers keyboard controls, starts/restarts play, cycles levels, exposes player health/steering in render_game_to_text, or shows health in the HUD status. Added a top-right icon fullscreen button that requests fullscreen on the app shell and maintains aria-pressed state. Added failing tests first for passive app behavior, no gameplay listeners, simplified text state, and fullscreen button wiring. Verification passed: targeted app/state/fullscreen tests, npm run typecheck, npm run test:run with 98 tests, and npm run build. Web-game capture ran at http://127.0.0.1:5173/?fresh=20260523-demo-only-fullscreen with screenshot/state in output/web-game/demo-only-fullscreen; state reports mode demo, level `cinquefoil-knot`, camera progress/curvature/torsion, and no player payload. Browser click verification confirmed the fullscreen button entered fullscreen on `#app-shell`; the in-app browser was refreshed to http://127.0.0.1:5173/?fresh=20260523-demo-only-fullscreen-live afterward.
- Normal-plane up-axis correction: user clarified that "up" in the normal-plane inset should match the up direction in the tunnel view. Added failing HUD and sampler tests first. The curve sampler now stores a finite-difference `tangentDerivative` vector, and the HUD projects `T'` plus projected `N'` into the current tunnel view frame using the current normal as screen up and tangent-cross-up as screen right. The live readout also uses the current camera/player frame rather than only the nearest sampled frame. Verification so far: targeted HUD/sampler tests passed, npm run typecheck passed, npm run test:run passed with 100 tests, and npm run build passed.
- Retro arc meter pass: replaced the top red/yellow/green horizontal band meters with neutral retro-futuristic arc gauges. Added failing tests first for unsigned curvature arc mapping and signed torsion mapping with zero at the top center. Implemented shared `calculateArcMeterGeometry`, widened the gauges with elliptical arcs, gave desktop top panels more height, removed the lower accent line from meter panels so it no longer cuts through the needles, and kept the numeric readouts as secondary text. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 102 tests, and npm run build. Browser visual inspection used http://127.0.0.1:5173/?fresh=20260523-retro-arc-meters-final.
- Three issue fix pass: fixed user-reported arc meter, tunnel jitter, and fullscreen button issues. Added failing tests first for constant visual needle length, continuous tunnel ring movement within a nearest-sample interval, and fullscreen fallback when the browser lacks fullscreen APIs. Needles now use a constant circular endpoint length instead of ellipse radii; tunnel rings and wall are sampled by continuous arc length via `sampleAtArcLength` instead of integer nearest-sample indices; fullscreen button now supports missing API fallback, webkit-prefixed APIs, active pressed styling, and `is-fullscreen-fallback` shell styling. `render_game_to_text` HUD metadata now reports `meterStyle: neutral-retro-arc` instead of stale blue/green meter colors. Verification passed: targeted HUD/tunnel/fullscreen/state tests, npm run typecheck, npm run test:run with 105 tests, npm run build, web-game screenshot/state capture in output/web-game/three-bug-fixes, and in-app browser click verification at http://127.0.0.1:5173/?fresh=20260523-three-bug-fixes-final showing fallback=true and aria-pressed=true in the no-fullscreen-API environment.
- Arc meter bounds fix: user reported the needle bases and arc ends were outside the top HUD panels. Root cause was the arc meter draw path placing the gauge center below the panel and using a vertical radius larger than the panel height. Added a failing HUD geometry regression, then centralized the bounded layout in `calculateArcMeterLayout` and updated drawing to use it. Browser visual inspection at http://127.0.0.1:5173/?fresh=20260523-bounded-arc-meters shows pivots and arc endpoints inside the allocated frames. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 106 tests, and npm run build.
- Vector-colored meter fix: user requested curvature gauge colors match the blue `T'` vector and torsion gauge colors match the green `N'` vector. Added a failing HUD style test first, then introduced `calculateArcMeterVisualStyle` and fed the selected style into the arc, ticks, labels, needle, pivot, and numeric readout. `render_game_to_text` HUD metadata now reports `meterStyle: vector-colored-retro-arc`. Browser visual inspection at http://127.0.0.1:5173/?fresh=20260523-colored-arc-meters showed the curvature scale/needle in blue and torsion scale/needle in green. Verification passed: focused HUD/state tests, npm run typecheck, npm run test:run with 107 tests, and npm run build.
- Center status removal: user requested removing the center/top `4/4 Cinquefoil Knot XX%` message. Replaced stale status placement tests with an explicit empty center status formatter regression, removed the `drawStatus` call and status drawing methods from the HUD overlay, and verified visually at http://127.0.0.1:5173/?fresh=20260523-no-center-status that the level/progress message is no longer present. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 105 tests, and npm run build.
- Minimap depth pass: user requested removing the word `map` from the bottom-left inset and adding subtle 3D cues for knot crossings. Added failing HUD helper tests first for the empty minimap label, z-depth normalization, and depth-sorted stroke styling. The minimap now draws closed-curve segments sorted by normalized z depth, with raised segments brighter/thicker and drawn over a small black underlay so over/under crossings read more clearly. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 108 tests, npm run build, and web-game screenshot/state capture in output/web-game/minimap-depth showing no `MAP` label and a depth-shaded Cinquefoil Knot minimap.
- Minimap performance/style correction: user reported the app became slow and the minimap looked like thick high-z bands instead of the old view with crossing info. Root cause was the prior minimap renderer replacing one cheap path stroke with per-sample shadowed strokes and variable width by z. Added failing HUD tests first for thin near-uniform minimap stroke styling and projected crossing cue detection. Restored the old single thin cyan path, then added crossing-only over/under cues: small black gaps for underpasses and short bright cyan overpass strokes. Browser FPS check at http://127.0.0.1:5173/?fresh=20260523-minimap-crossing-cues-fps measured about 60 fps; the only console error was the existing missing favicon 404. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 109 tests, npm run build, and web-game screenshot/state capture in output/web-game/minimap-crossing-cues.
- Minimap crossing clarity follow-up: user reported the over/under crossings were still not clear. Root cause: the detector only found exact projected line intersections, while the Cinquefoil minimap mostly has very close projected passes with strong z-depth differences. Added failing HUD tests first for close-pass cue detection and readable bridge marker geometry. Crossing cues now rank close non-adjacent strand passes by screen distance, dedupe nearby candidates, and draw each selected crossing as a local bridge marker with a longer underpass gap, outlined over-strand, shoulder ticks, and a small center highlight. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 111 tests, npm run build, screenshot in output/web-game/minimap-near-crossings-dedupe, and browser FPS check at http://127.0.0.1:5173/?fresh=20260523-minimap-near-crossings-fps measured about 60 fps.
- Minimap tick cleanup: user reported the bridge marker shoulder ticks looked like weird little tick marks along the curve. Added a failing HUD marker regression first, then removed the perpendicular shoulder strokes while keeping the underpass gap, outlined overpass segment, and small center highlight. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 111 tests, npm run build, and browser screenshot in output/web-game/minimap-no-ticks.
- Minimap mask-only crossing cleanup: user liked the dark undercrossing masks but reported the bright overcrossing segments did not follow the knot. Added a failing HUD marker regression first, then removed all visible overpass span/highlight fields and drawing. Crossing cues now only mask undercrossings; the normal thin cyan knot path supplies the overstrand. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 111 tests, npm run build, and browser screenshot in output/web-game/minimap-mask-only.
- Minimap z-brightness reset: user rejected crossing masks and requested brightness at each curve point be proportional to z value. Added a failing HUD regression first for a wide z-driven alpha range with constant stroke width. Removed the crossing cue/mask helpers and renderer path entirely. The minimap now draws a faint continuous base curve plus constant-width cyan segment buckets whose opacity increases with normalized z depth. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 108 tests, npm run build, browser screenshot in output/web-game/minimap-z-brightness, and browser FPS check at http://127.0.0.1:5173/?fresh=20260523-minimap-z-brightness-fps measured about 60 fps.
- Gauge pivot repositioning: user requested curvature/torsion needle pivots move below the visible gauge areas while only visible needle portions are shown. Added failing HUD geometry tests first for below-panel pivots and clipped visible needle segments. Updated the retro arc angles/layout so the scale remains visible with the pivot below the panel, added rectangular line clipping for needles, and removed the visible pivot hub. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 109 tests, npm run build, and browser screenshot in output/web-game/gauge-pivots-below-clipped.
- Hybrid cockpit HUD pass: user selected mockup option A with option B's bottom-center curvature/torsion history graph. Added failing tests first for demo invariant history, connected top meter canopy geometry, bottom-center history panel placement, and graph coordinate mapping. Implemented rolling `invariantHistory` in demo simulation, replaced the two separate top meter panels with a shared connected canopy, added foreground cockpit occlusion plates over the tunnel view, and converted the lower center HUD area into a live blue/green recent-invariants graph. Verification passed: focused player/HUD tests, npm run typecheck, npm run test:run with 113 tests, npm run build, and web-game screenshot/state capture in output/web-game/hybrid-hud-history-final.
- HUD correction pass: fixed follow-up visual issues by making the torsion history graph use a centered zero line, changing the connected top meter window to a shallower A-style bridge, removing the foreground center occlusion plates, enlarging the bottom-left and bottom-right insets by about 1.5x on desktop, and keeping the bottom-center invariant graph at its previous compact size. Added failing HUD helper tests first for enlarged corner pods, shallow canopy geometry, torsion zero line metadata, and compact center graph height. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 115 tests, npm run build, and web-game screenshot/state capture in output/web-game/hud-corrections-20260523.
- Bottom HUD balance follow-up: user wanted bottom-left and bottom-right inset sizes to match, with a slightly wider center graph, and allowed reducing the corner inset size if necessary. Added failing HUD layout tests first for equal corner pod dimensions and a wider center history panel. Tuned corner pods to about 1.35x the legacy radar size on desktop, kept left/right equal, and widened the center graph modestly. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 116 tests, npm run build, and web-game screenshot/state capture in output/web-game/hud-inset-balance-20260523b. In-app browser refreshed to http://127.0.0.1:5173/?fresh=hud-inset-balance-20260523b.
- Bottom-center title removal: user requested removing the title from the bottom-center invariant graph. Added a failing HUD formatter test first, introduced `formatInvariantHistoryLabel()` returning an empty string, and routed the graph title through it so the `RECENT INVARIANTS` text no longer draws while `KAPPA` and `TAU` remain. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 117 tests, npm run build, and web-game screenshot/state capture in output/web-game/no-center-invariant-title-20260523. In-app browser refreshed to http://127.0.0.1:5173/?fresh=no-center-invariant-title-20260523.
- Gauge needle shortening: user requested reducing the curvature/torsion needle length so tips do not cross the gauge arcs. Added failing HUD geometry regressions first: the original length overshot the ellipse boundary, and the first fix still left too little visual gap once glow/line caps were considered. `calculateArcMeterLayout()` now computes needle length from the nearest arc boundary and subtracts a larger visual margin. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 118 tests, npm run build, and web-game screenshot/state capture in output/web-game/shorter-gauge-needles-20260523b. In-app browser refreshed to http://127.0.0.1:5173/?fresh=shorter-gauge-needles-20260523b.
- Torsion history headroom and legend cleanup: user reported the torsion history graph was still bottoming out and asked to remove `KAPPA`/`TAU`. Root cause was signed torsion graph scaling using the largest observed torsion magnitude directly, so extrema landed on the graph rails. Added failing HUD tests first for torsion graph headroom and empty legend labels, then applied 1.5x torsion history headroom and removed the bottom-center legend text. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 120 tests, npm run build, and web-game screenshot/state capture in output/web-game/torsion-history-headroom-20260523. In-app browser refreshed to http://127.0.0.1:5173/?fresh=torsion-history-headroom-20260523.
- Orange tunnel/knot palette pass: user requested the tunnel rings and bottom-left inset knot change to orange. Added failing tests first for the tunnel material color and minimap stroke color formatter, then changed `hudColors.tunnelRing` and the minimap brightness stroke path to the shared orange palette while leaving HUD frames/meters/normal-plane colors unchanged. Verification passed: focused HUD/tunnel tests, npm run typecheck, npm run test:run with 120 tests, npm run build, and web-game screenshot/state capture in output/web-game/orange-tunnel-knot-20260523. In-app browser refreshed to http://127.0.0.1:5173/?fresh=orange-tunnel-knot-20260523.
- Top canopy alignment pass: user requested the top shared meter window be narrower, with its left side vertically aligned to the bottom-left inset and its right side aligned to the bottom-right inset. Added a failing HUD layout test first, then changed `calculateConnectedMeterCanopy()` to use bottom inset bounds and shifted the top meter right edge inside that same bound. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 121 tests, npm run build, and web-game screenshot/state capture in output/web-game/aligned-top-canopy-20260523. In-app browser refreshed to http://127.0.0.1:5173/?fresh=aligned-top-canopy-20260523.
- Red inset border/guide pass: user requested red borders around all insets, then clarified the normal-plane background guide curves, minimap guide, and the small top-center canopy guide line should also be red. Added failing HUD style tests first for shared red inset borders and guide-line formatters. Switched inset border/accent strokes to shared red, tuned the red to `#ff1414` after visual feedback, and routed the bottom-left minimap guide, bottom-right normal-plane guide curves, bottom-center history guide lines, and top canopy guide line through the same red guide-line style. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 124 tests, npm run build, and web-game screenshot/state capture in output/web-game/red-insets-guides-20260523. In-app browser refreshed to http://127.0.0.1:5173/?fresh=red-insets-guides-20260523.
- Torsion graph rail-margin follow-up: user reported the bottom-center torsion trace still appeared to bottom out, then noted it might be a misread. Before stopping, tightened the HUD graph regression to require a larger pixel rail margin and changed `calculateInvariantHistoryGraph()` to reserve explicit pixel headroom for signed torsion instead of relying on a proportional scale multiplier. Fresh screenshot in output/web-game/torsion-debug-current-20260523 shows the green torsion trace sloping near the lower guide rather than clipping flat. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 124 tests, npm run build, and in-app browser refreshed to http://127.0.0.1:5173/?fresh=torsion-debug-current-20260523.
- Meter digital readout removal: user requested removing the digital curvature/torsion value readouts just to the right of the top meters. Added a failing HUD formatter test first, introduced `formatArcMeterDigitalReadout()` returning an empty string, and routed top meter numeric drawing through that formatter so the right-side values no longer draw while labels, ticks, and needles remain. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 125 tests, npm run build, and web-game screenshot/state capture in output/web-game/no-meter-digital-readouts-20260523. In-app browser refreshed to http://127.0.0.1:5173/?fresh=no-meter-digital-readouts-20260523.
- Chrome fullscreen follow-up: user reported the fullscreen button did not work in Chrome. Reproduced that Chromium entered fullscreen but did not emit a resize event, leaving the app dependent on stale canvas sizing. Added failing fullscreen/app tests first, then changed the fullscreen button to request fullscreen on `document.documentElement`, recognize root fullscreen as active, and invoke an app resize callback on fullscreen changes. `FreneticApp` now exposes an explicit `resize()` hook, and main schedules immediate plus next-frame resize after fullscreen changes. Verification passed: focused fullscreen/app tests, npm run typecheck, npm run test:run with 127 tests, npm run build, live Chromium fullscreen probe reporting fullscreenTag `HTML` and aria-pressed `true`, and web-game screenshot/state capture in output/web-game/fullscreen-root-resize-20260524. In-app browser refreshed to http://127.0.0.1:5173/?fresh=fullscreen-root-resize-final-20260524.
- Top meter label padding follow-up: user requested moving the `CURVATURE` and `TORSION` labels right so their first letters have breathing room from the angular window border. Added a failing HUD helper test first, introduced `calculateArcMeterTitlePosition()` with a 36px left inset, and routed top arc meter label drawing through it. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 128 tests, npm run build, and web-game screenshot/state capture in output/web-game/meter-label-padding-20260524. In-app browser refreshed to http://127.0.0.1:5173/?fresh=meter-label-padding-20260524.
- Bottom bridge mock-up: user asked to try bridge geometry between the bottom-center invariant history window and the two bottom corner windows before implementation. Created a standalone visual mock-up at .superpowers/brainstorm/bottom-bridges-20260524/content/bottom-bridge-mockup.html using the latest app screenshot as a base, with red angular filled bridge panels overlaid. Browser preview URL used Vite's /@fs path, and a rendered PNG was saved to bottom-bridge-mockup-20260524.png. No app implementation files were changed for this mock-up.
- Straight flared bridge mock-up: user asked to see straight bridges with flared ends. Added a second standalone mock-up at .superpowers/brainstorm/bottom-bridges-20260524/content/straight-flared-bridges.html with horizontal connector bars, trapezoidal flared end caps, subtle internal red linework, and the same latest app screenshot base. Rendered PNG saved to straight-flared-bridges-20260524.png. No app implementation files were changed.
- Hourglass bridge mock-up: user clarified the straight bridge flares were oriented wrong and each bridge should read as hourglass-shaped. Added a third standalone mock-up at .superpowers/brainstorm/bottom-bridges-20260524/content/hourglass-bridges.html with wide panel-contact ends and a pinched waist in each connector. Rendered PNG saved to hourglass-bridges-20260524.png. No app implementation files were changed.
- Sketch-style bridge mock-up: user provided a photo of the desired bridge shape. Added a fourth standalone mock-up at .superpowers/brainstorm/bottom-bridges-20260524/content/sketch-style-bridges.html with vertical outer end faces, diagonal shoulders, and a narrow straight central throat matching the sketch more closely. Rendered PNG saved to sketch-style-bridges-20260524.png. No app implementation files were changed.
- Straightened sketch bridge mock-up: user said the sketch-style bridges were the right idea but too wiggly. Added a fifth standalone mock-up at .superpowers/brainstorm/bottom-bridges-20260524/content/straight-sketch-bridges.html with straight horizontal throat bars, symmetric diagonal shoulders, vertical end faces, and reduced internal linework. Rendered PNG saved to straight-sketch-bridges-20260524.png. No app implementation files were changed.
- Bottom bridge implementation: user approved the straightened sketch bridge concept and requested identical left/right bridges that are each left-right symmetric. Added failing HUD geometry tests first, then introduced `calculateBottomBridgePanels()` from one canonical symmetric shape and rendered two identical bridge instances between the lower windows. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 129 tests, npm run build, and web-game screenshot/state capture in output/web-game/bottom-bridges-20260524. In-app browser refreshed to http://127.0.0.1:5173/?fresh=bottom-bridges-20260524.
- Bottom bridge black fill follow-up: user requested black bridge backgrounds so the three bottom windows and bridges read as one continuous lower region. Added a failing HUD style test first, introduced `calculateBottomBridgeVisualStyle()` returning `hudColors.panelFill` plus red stroke, and replaced the prior translucent red bridge fill with solid black. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 130 tests, npm run build, and web-game screenshot/state capture in output/web-game/black-bottom-bridges-20260524. In-app browser refreshed to http://127.0.0.1:5173/?fresh=black-bottom-bridges-20260524.
- Side vertical guide pass: user requested subtle vertical red lines on the left/right sides between the top inset and lower windows, then asked to make them less subtle. Added failing HUD guide placement/opacity tests first, introduced `calculateSideVerticalHudGuides()` and drew dashed red side lines from below the top canopy to above the lower corner panels. Increased guide alpha from 0.26 to 0.50 and stroke width to 1.35 after the first visual capture was too faint. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 131 tests, npm run build, and web-game screenshot/state capture in output/web-game/darker-side-vertical-guides-20260524. In-app browser refreshed to http://127.0.0.1:5173/?fresh=darker-side-vertical-guides-20260524.
- Side vertical guide refinement: user requested the side guide lines be solid, match the minimap guide curve brightness/thickness, and move inward past the upper canopy chamfers. Updated the guide placement test to pin the x positions to `canopy.x + canopyCut + 10` and the mirrored right edge, added `calculateSideVerticalHudGuideStyle()` returning the same stroke color as `formatMinimapGuideLineColor()` with 1px width and no dash, then updated drawing to use that style. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 131 tests, npm run build, and web-game screenshot/state capture in output/web-game/solid-inset-side-guides-20260524. In-app browser refreshed to http://127.0.0.1:5173/?fresh=solid-inset-side-guides-20260524.
- Side vertical guide radar-match follow-up: user said the vertical side lines still looked too light and should match the background curves in the radar/normal-plane view. Root cause was the side guide draw path still applying a red shadow/glow even though the nominal stroke color and width matched. Added a failing HUD style assertion for `shadowBlur: 0`, updated `calculateSideVerticalHudGuideStyle()` and drawing to remove the glow, and verified the rendered side lines now use the same non-glowing `rgba(255, 20, 20, 0.480)`/1px treatment as the radar guide curves. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 131 tests, npm run build, and web-game screenshot/state capture in output/web-game/minimap-matched-side-guides-20260524b. In-app browser refreshed to http://127.0.0.1:5173/?fresh=minimap-matched-side-guides-20260524b.
- Side vertical guide visual-weight follow-up: user provided a crop showing the side vertical line still read lighter than the inset background curves. Added a failing style assertion for stronger visual compensation, kept the half-pixel crisp placement and no glow, then increased the side guide stroke to `rgba(255, 20, 20, 0.720)` and `1.2` width so it reads closer to the radar/minimap guide curves in context. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 131 tests, npm run build, and web-game screenshot/state capture in output/web-game/stronger-side-guides-20260524. In-app browser refreshed to http://127.0.0.1:5173/?fresh=stronger-side-guides-20260524.
- Cinquefoil torus-normal variation pass: user approved adding variation in the torus-normal direction to the demo knot. Added a failing sampler regression proving the Cinquefoil no longer stays on one constant-radius torus tube while remaining closed, then introduced `normalVariedTorusKnot()` and changed only the `cinquefoil-knot` curve to use a mild periodic tube-radius variation (`amplitude 0.38`, frequency 3) around the same `(p, q) = (2, 5)` torus-knot angles. Verification passed: focused sampler tests, npm run typecheck, npm run test:run with 132 tests, npm run build, and web-game screenshot/state capture in output/web-game/varied-cinquefoil-20260524. In-app browser refreshed to http://127.0.0.1:5173/?fresh=varied-cinquefoil-20260524.
- Figure-eight knot replacement: user approved switching the default demo knot to the standard figure-eight parameterization `((2 + cos 2a) cos 3a, (2 + cos 2a) sin 3a, sin 4a)`. Added failing tests first for authored level metadata, known sample points, and default demo state, then replaced the fourth level with `figure-eight-knot` / `Figure-Eight Knot` at scale 2.2. Verification passed: focused curve/state/app tests, npm run typecheck, npm run test:run with 132 tests, npm run build, and web-game screenshot/state capture in output/web-game/figure-eight-knot-20260524. In-app browser refreshed to http://127.0.0.1:5173/?fresh=figure-eight-knot-20260524.
- Cinquefoil restore: user rejected the experimental `(2,3)` cable visual and requested returning to the Cinquefoil. Added failing expectations for `cinquefoil-knot`, restored the fourth authored level to the normal-varied Cinquefoil curve, and removed the cable/figure-eight helper geometry from the active level definition. Verification passed: focused curve/state/app tests, npm run typecheck, npm run test:run with 132 tests, npm run build, and web-game screenshot/state capture in output/web-game/back-to-cinquefoil-20260524. In-app browser refreshed to http://127.0.0.1:5173/?fresh=back-to-cinquefoil-20260524.
- Tangent-Binormal plane and flat main-vector pass: moved the bottom-right inset from `NORMAL PLANE` to `TANGENT-BINORMAL PLANE`, with red T/B axes, orange `N'`, blue T projection, and green B projection. Added shared Frenet vector math for `T'` and `N' = -kappa T + tau B`. Initially implemented main-view arrows as Three.js world vectors, then corrected them per user feedback to flat 2D HUD vectors overlaid on the tunnel view. Verification passed: npm run typecheck, npm run test:run with 137 tests, npm run build, and web-game screenshot/state capture in output/web-game/flat-main-vectors-tb-plane-20260524. In-app browser refreshed to http://127.0.0.1:5173/?fresh=flat-main-vectors-tb-plane-20260524.
- Main-view vector length tuning: user requested doubling the flat `T'`/`N'` overlay vector lengths. Added a HUD regression expecting the doubled endpoint and max length, then changed the main-view vector length ratio from 12% to 24% of the smaller viewport dimension with a doubled minimum. Verification passed: npm run typecheck, npm run test:run with 137 tests, npm run build, and web-game screenshot/state capture in output/web-game/double-main-vectors-20260524. In-app browser refreshed to http://127.0.0.1:5173/?fresh=double-main-vectors-20260524.
- Main-view N-prime color tuning: user requested `N'` in the tunnel view only be green. Added a HUD style regression proving main-view `N'` is green while the Tangent-Binormal inset `N'` remains orange, then routed both draw paths through explicit style helpers. Verification passed: npm run typecheck, npm run test:run with 138 tests, npm run build, and web-game screenshot/state capture in output/web-game/main-nprime-green-20260524. In-app browser refreshed to http://127.0.0.1:5173/?fresh=main-nprime-green-20260524.
- Fourier knot trial and tunnel motion fix: user requested trying a Fourier knot, then reported the first version created dark self-occluding tunnel patches and rings that did not read as moving toward the camera. Added failing sampler tests for a closed asymmetric `fourier-knot` default with bounded curvature and signed torsion variation, plus a tunnel regression proving ring distances decrease relative to the camera between lattice resets. Replaced the fourth authored level with a smoother finite Fourier series, tightened its curvature/torsion metadata, and anchored tunnel rings to a world-space arc-length lattice instead of fixed camera-relative offsets. Verification passed: focused curve/tunnel/app/state tests, npm run typecheck, npm run test:run with 139 tests, npm run build, and web-game screenshot/state capture in output/web-game/fourier-knot-smoothed-20260524. In-app browser refreshed to http://127.0.0.1:5173/?fresh=fourier-knot-smoothed-20260524.
- Fixed main-vector center follow-up: user preferred the `T'`/`N'` overlay base not wander. Removed the attempted projected-ring-center anchor and restored the main-view derivative vectors to the stable viewport center while keeping the tunnel ring lattice motion fix. Verification passed: focused HUD/gameRenderer/tunnel tests, npm run typecheck, npm run test:run with 139 tests, npm run build, and web-game screenshot/state capture in output/web-game/fixed-main-vector-center-20260524. In-app browser refreshed to http://127.0.0.1:5173/?fresh=fixed-main-vector-center-20260524.
- Granny knot trial: user switched from the Lissajous request to trying Paul Bourke's Granny knot parameterization. Replaced the partially edited Lissajous fourth level with a scaled Bourke Granny knot (`granny-knot` / `Granny Knot`), set speed to 2.6, curvature metadata to `[0, 0.35]`, and torsion metadata to `[-0.09, 0.09]`. Added/updated sampler/app/state expectations for the new default, including a source-formula sample point at `u = 0` and signed torsion/curvature bounds. Verification passed: focused curve/app/state tests, npm run typecheck, npm run test:run with 139 tests, npm run build, and web-game screenshot/state capture in output/web-game/granny-knot-20260524. In-app browser refreshed to http://127.0.0.1:5173/?fresh=granny-knot-20260524.
- Triple derivative vector display scale: user requested tripling the vector lengths in the radar/Tangent-Binormal view and tunnel view. Added a shared 3x derivative-vector display scale for Tangent-Binormal plane normalization and increased the main-view overlay max/min vector length by 3x (`0.24 -> 0.72` viewport ratio, `72 -> 216` minimum). Kept inset vectors clamped to the panel radius for large values. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 139 tests, npm run build, and web-game screenshot/state capture in output/web-game/triple-derivative-vectors-20260524. In-app browser refreshed to http://127.0.0.1:5173/?fresh=triple-derivative-vectors-20260524.
- Tangent-Binormal title padding: user requested moving the bottom-right `TANGENT-BINORMAL PLANE` title down a few pixels for more top padding. Added `calculateTangentBinormalPlaneTitlePosition()` and moved the title baseline from `rect.y + 18` to `rect.y + 24` while preserving x alignment. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 140 tests, npm run build, and web-game screenshot/state capture in output/web-game/tb-title-padding-20260524. In-app browser refreshed to http://127.0.0.1:5173/?fresh=tb-title-padding-20260524.
- Greek invariant labels: user requested blue `κ` labels near the curvature meter and top-left of the bottom-center graph, plus green `τ` labels near the torsion meter and top-right of the graph. Added positioning helpers for meter symbols and history graph symbols, then removed glow from the Greek labels after feedback and switched them to a serif Greek-friendly font so `κ` reads clearly. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 143 tests, npm run build, and web-game screenshot/state capture in output/web-game/no-glow-greek-labels-serif-20260524. In-app browser refreshed to http://127.0.0.1:5173/?fresh=no-glow-greek-labels-serif-20260524.
- Near-ring skip reduction: user asked to partially fix foreground tunnel rings disappearing by no longer skipping the first four rings. Set `NEAR_RING_SKIP` from `4` to `0` and updated tunnel geometry tests to expect the first visible ring on the current arc-length lattice instead of four intervals ahead. Verification passed: focused tunnel tests, npm run typecheck, npm run test:run with 143 tests, npm run build, and web-game screenshot/state capture in output/web-game/no-near-ring-skip-20260524. In-app browser refreshed to http://127.0.0.1:5173/?fresh=no-near-ring-skip-20260524.
- Fixed top meter domains: user requested the curvature gauge scale max be `0.4` and the torsion gauge range be `-0.1` to `0.1`. Added exported HUD scale constants, routed the top arc gauges through those fixed domains, and added a HUD regression for the resulting tick labels. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 144 tests, npm run build, and web-game screenshot/state capture in output/web-game/fixed-meter-domains-20260524. In-app browser refreshed to http://127.0.0.1:5173/?fresh=fixed-meter-domains-20260524.
- Tangent-Binormal N-prime color/label pass: user requested the `N'` vector and label in the radar/Tangent-Binormal view only become white, with no label glow and a label farther from the radar center. Changed only the Tangent-Binormal vector style to white, added per-label draw options so the bottom-right `N'` label uses zero shadow blur and a larger outward offset, and kept the main tunnel `N'` overlay green. Verification passed: focused HUD tests, npm run typecheck, npm run test:run with 145 tests, npm run build, and web-game screenshot/state capture in output/web-game/white-radar-nprime-20260524. In-app browser refreshed to http://127.0.0.1:5173/?fresh=white-radar-nprime-20260524.
- Chrome fullscreen follow-up: user reported the fullscreen button still failed in Chrome. Investigation showed the in-app browser could enter fullscreen, but the implementation was requesting fullscreen on `document.documentElement` rather than the app shell. Changed native fullscreen requests to target `#app-shell`, pass Chrome's `{ navigationUI: 'hide' }` hint, and run fullscreen resize/state sync when Chrome resolves the request promise as well as on `fullscreenchange`. Verification passed: focused fullscreen tests, npm run typecheck, npm run test:run with 147 tests, npm run build, live browser click reporting `fullscreenElementId: "app-shell"`, and web-game screenshot/state capture in output/web-game/chrome-shell-fullscreen-20260524. In-app browser refreshed to http://127.0.0.1:5173/?fresh=chrome-shell-fullscreen-20260524.
- Fullscreen icon placement: user requested moving the fullscreen icon to the center of the top screen edge. Changed the fixed button CSS from top-right anchoring to `left: 50%` with horizontal translation. Verification passed: npm run build and web-game screenshot capture in output/web-game/center-fullscreen-icon-20260524. In-app browser refreshed to http://127.0.0.1:5173/?fresh=center-fullscreen-icon-20260524.
- Repo cleanup pass: user said the app is done and requested cleanup/removal of unused code. Removed obsolete playable-game modules and tests (`input/pointerTrackpad`, steering smoothing, collision, nearest-centerline lookup), simplified simulation state/config to passive demo fields, removed unused level visual/steering metadata, pruned stale HUD helper/drawing code, excluded ignored `.worktrees` from Vitest discovery, and deleted generated root PNG/mockup/.DS_Store artifacts plus an untracked superpowers plan. Verification passed: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, npm run typecheck, npm run test:run with 94 app tests, npm run build, and web-game screenshot/state capture in output/web-game/cleanup-final-20260524. In-app browser refreshed to http://127.0.0.1:5173/?fresh=cleanup-final-20260524.
- Minimap tangent-vector pass: user requested a white tangent vector labeled `T` in the minimap view. Added a failing HUD helper test first, introduced `calculateMinimapTangentVector()` to project the current Frenet tangent into the minimap screen plane with fixed display length, and drew the white `T` arrow from the current minimap dot. Verification passed: focused HUD test, `npm run typecheck`, `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run test:run` with 95 tests, `npm run build`, and web-game screenshot/state capture in output/web-game/minimap-tangent-vector-20260525.
- Tunnel-view label sizing pass: user requested increasing the labels in the tunnel view. Added a failing HUD helper test first, introduced `calculateMainViewDerivativeVectorLabelOptions()` with a 16px font size, and applied it only to the main tunnel-view `T'`/`N'` overlay vectors. Verification passed: focused HUD test, `npm run typecheck`, `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run test:run` with 96 tests, `npm run build`, and web-game screenshot/state capture in output/web-game/tunnel-label-font-size-20260525. In-app browser refreshed to http://127.0.0.1:5173/FRENETic/?fresh=tunnel-label-font-size-20260525.
- Minimap tangent-label clipping pass: user reported the minimap `T` label briefly leaving the right edge and asked not to use superpowers. Added focused HUD tests for visible and overflowing label states, then made `calculateMinimapTangentVector()` mark the label invisible when its text box would leave the minimap panel. The arrow remains visible; only the label is suppressed. Verification passed: focused HUD test, `npm run typecheck`, `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run test:run` with 97 tests, `npm run build`, and web-game screenshot/state capture in output/web-game/minimap-t-label-clip-20260525. In-app browser refreshed to http://127.0.0.1:5173/FRENETic/?fresh=minimap-t-label-clip-20260525.
- Fullscreen compatibility pass: user reported Chrome still did not enter fullscreen and Safari fullscreen froze HUD/inset animation while the tunnel rings kept moving. Root-cause investigation found the fullscreen helper still preferred the inner app shell and passed Chrome-sensitive `navigationUI` options, while the dynamic HUD canvas texture used default mipmapping. Added failing regressions first, then changed native fullscreen to request the document root with a plain `requestFullscreen()` call and configured the HUD `CanvasTexture` with linear filtering and mipmaps disabled for dynamic fullscreen updates. Verification passed: focused fullscreen/HUD tests, `npm run typecheck`, `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run test:run` with 98 tests, `npm run build`, and in-app Chromium fullscreen verification showing `document.fullscreenElement.tagName === "HTML"` with demo progress/curvature changing while fullscreen. Screenshot saved in output/web-game/fullscreen-root-texture-20260525.
- Fullscreen DOM-HUD follow-up: Safari still froze HUD/inset animation in native fullscreen, so the HUD was moved out of the Three.js texture path into a real DOM `<canvas id="hud-canvas">` overlay above the WebGL canvas. Verification passed: `npm run typecheck`, `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run test:run` with 97 tests, `npm run build`, and in-app Chromium native fullscreen verification showing `document.fullscreenElement.tagName === "HTML"` with changing demo state and live HUD canvas repaint.
- Chrome fullscreen retry follow-up: user confirmed Safari was fixed but Chrome still did not enter fullscreen. Added a failing fullscreen-helper regression for trying an alternate native target after a rejected request, then changed the button to request fullscreen on the app shell first with no `navigationUI` option, try document root/body next, support both WebKit fullscreen spellings, and only use CSS fallback after all native candidates fail. Verification passed: focused fullscreen test, `npm run typecheck`, `npm run test:run` with 98 tests, `npm run build`, and Playwright browser verification showing `fullscreenElement: "MAIN"` after clicking the button.
